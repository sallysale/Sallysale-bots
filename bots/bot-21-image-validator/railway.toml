# railway.toml — BOT-21 Image Validator
[build]
builder = "nixpacks"

[deploy]
startCommand = "node bot21.mjs"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# cron: פעם ביום 04:00 UTC
# מוגדר ב-Railway dashboard → Service → Settings → Cron
# ערך: 0 4 * * *
