# railway.toml — BOT-30 Anti-Abuse
[build]
builder = "nixpacks"

[deploy]
startCommand = "node bot30.mjs"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# cron: כל 2 שעות
# מוגדר ב-Railway dashboard → Service → Settings → Cron
# ערך: 0 */2 * * *
