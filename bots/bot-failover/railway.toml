# railway.toml — BOT-Failover
[build]
builder = "nixpacks"

[deploy]
startCommand = "node botFailover.mjs"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# cron: כל שעה
# מוגדר ב-Railway dashboard → Service → Settings → Cron
# ערך: 0 * * * *
