# railway.toml — BOT-17 Price Validator
[build]
builder = "nixpacks"

[deploy]
startCommand = "node bot17.mjs"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# cron: כל שעה
# מוגדר ב-Railway dashboard → Service → Settings → Cron
# ערך: 0 * * * *
