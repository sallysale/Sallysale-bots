# railway.toml — BOT-20 Expiry Detector
[build]
builder = "nixpacks"

[deploy]
startCommand = "node bot20.mjs"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# cron: כל 6 שעות
# מוגדר ב-Railway dashboard → Service → Settings → Cron
# ערך: 0 */6 * * *
